const initialState = {}

const likes = (state = initialState, action) => {
  switch (action.type) {
    case '':
      return state
    case '':
      return state
    case '':
      return state
    default:
      return state
  }
}

export default likes
